# DESCOMENTAR ABAIXO APENAS SE QUISER FAZER TESTES
# Configuracao do Banco H2 
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect


# spring.jpa.hibernate.ddl-auto=update


# spring.h2.console.enabled=true

# # Configuraacao da Porta (Padrão 8080)
# server.port=8080
# spring.datasource.hikari.maximum-pool-size=20
# spring.datasource.hikari.minimum-idle=5


# server.tomcat.threads.max=200
# server.tomcat.threads.min-spare=10

# spring.datasource.hikari.data-source-properties.h2.url=jdbc:h2:mem:testdb;LOCK_MODE=3
########################################################################################

spring.application.name=backend-vuln
spring.profiles.active=local

# PostgreSQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.default_schema=security-platform-springboot

# MongoDB
spring.data.mongodb.uri=mongodb://localhost:27017/pi2_logs

